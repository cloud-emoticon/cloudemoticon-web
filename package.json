{
  "name": "cloudemoticon-web",
  "productName": "Cloud Emoticon",
  "main": "electron/main.js",
  "version": "1.6.1",
  "license": "MIT",
  "dependencies": {
    "@material-ui/core": "^4.9.1",
    "@material-ui/icons": "^4.9.1",
    "copy-to-clipboard": "^3.2.1",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "xml2js": "^0.4.23"
  },
  "devDependencies": {
    "electron": "^25.1.0",
    "react-scripts": "^2.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "start-headless": "export BROWSER=none && react-scripts start",
    "start-headless-win": "set BROWSER=none && react-scripts start",
    "electron": "export ELECTRON_START_URL=http://localhost:3000 && electron .",
    "electron-win": "set ELECTRON_START_URL=http://localhost:3000 && electron .",
    "build": "react-scripts build",
    "eject": "react-scripts eject"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
